# Example environment variables (copy to .env and edit as needed)
APP_PORT=8000
APP_HOST=127.0.0.1
# Leave empty unless you need CORS for non-local origins
CORS_ORIGINS=http://127.0.0.1:8000,http://localhost:8000